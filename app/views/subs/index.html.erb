<!--
<h1>Subs#index</h1>
<p>Find me in app/views/subs/index.html.erb</p>


<h1>科目一覧</h1>

<% @subs.each do |sub| %>
    <h1><%= sub.name %></h1>
    <p><%= sub.kind %></p>
  <% end %>
!-->

  <h1>科目一覧</h1>
<table>
<thead>
  <tr>
    <th>科目名</th>
    <th>入出残別</th>
    <th colspan="3"></th>
  </tr>
</thead>

<% @subs.each do |sub| %>
<tr>
  <td><%= sub.name %></td>
  <td><%= sub.kind %></td>
  <td><%= link_to 'Show', sub %></td>
  <td><%= link_to 'Edit', edit_sub_path(sub) %></td>
  <td><%= link_to 'Destroy', sub, method: :delete,
                                   data: { confirm: 'Are you sure?' } %></td>
</tr>
<% end %>
</table>

<br>


<%= link_to "Homeへ",  :controller => 'trys', :action => 'home' %>
<%= link_to '科目登録へ', new_sub_path %>