<%= form_for(@day) do |f| %>
    
    <div class="field">
        <%= f.label :日付 %>
        <%= f.date_select :date %>
    </div>

    
    <div class="field">
        <!--
        <label id='subs_id'>科目</label>
        
        <%= f.label :subs_id, '科目' %>
            <select name="subs_id">
                <% @subs.each do |sub| %> 
                    <% end %> <%= f.text_field :name %>:view_name_and_id
        !-->            
       
    <%= f.label :subs_id %>
    <%= f.collection_select(:subs_id, @subs, :id, :name) %>
   
    </div>
    
    
    <div class="field">
        <%= f.label :金額 %>
        <%= f.number_field :value %>
    </div>

    <div class="field">
        <%= f.label :摘要 %>
        <%= f.text_field :discription %>
    </div>

    <div class="actions">
        <%= f.submit %>
    </div>

<% end %>

                